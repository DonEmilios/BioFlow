# Copy this file to .env and adjust as needed.

# URL of the local compute backend (see server/). Leave unset to fall back
# to fully in-browser mock execution — useful for UI-only development, but
# custom nodes and real file-based execution require the backend running.
VITE_COMPUTE_BACKEND_URL="http://localhost:8787"
